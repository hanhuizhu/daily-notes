---
description: AI代码生成标准(v10.0.33)
alwaysApply: true
priority: 9999
---
# AI代码生成标准【必须立即执行】

## 核心原则【强制执行】
1. **场景识别优先**：每次用户交互必须先识别开发场景
2. **规则读取执行**：识别到场景后立即读取并执行对应规则
3. **标准严格遵循**：所有代码生成必须遵循第二部分：代码生成标准
4. **严格场景匹配**：只能匹配已声明的场景，禁止创建或编造其他场景

---

## 第一部分：场景识别与执行【必须立即执行】

### 执行步骤【强制顺序】
```
用户输入 → 场景识别 → 规则读取 → 立即执行
```

### 场景识别清单【严格匹配】

**识别原则：**
- 按顺序逐一检查用户输入，只匹配已明确定义的场景
- 一旦匹配到场景，立即执行对应规则，不再继续匹配
- 禁止创建或编造本文档未明确定义的场景

#### 🆕 新页面开发
**触发条件：** 新页面开发
**执行动作：**
1. 调用 `fetch_rules(["scene/new-page-rule", "modules/common-page"])`
2. 分析页面类型：
   - 列表页面（数据展示、表格、查询列表） → 追加 `fetch_rules(["modules/scene-list"])`
   - 表单页面（数据录入、编辑表单、新增表单） → 追加 `fetch_rules(["modules/scene-form"])`

#### 🔄 JS转TS场景
**触发条件：** JS转TS、添加类型声明、类型系统优化
**执行动作：** 调用 `fetch_rules(["scene/js-to-ts"])`

#### 🎨 Mars-PC组件库迁移
**触发条件：** 组件库替换、UI组件迁移、Mars-PC重构
**执行动作：** 调用 `mars-pc-migration-skill`

#### 🔗 Forseti接口集成
**触发条件：** 只提供了Forseti接口地址
**执行动作：** 调用 `fetch_rules(["modules/004-forseti-rules", "modules/mbf-docs"])`

#### 📝 生成PRD文档
**触发条件：** 根据代码生成产品需求文档
**执行动作：** 调用 `fetch_rules(["scene/generate-prd"])`

#### 📋 MDC规则生成
**触发条件：** 生成mdc规则、创建cursor规则、编写规则文档、新增规则文件
**执行动作：** 调用 `fetch_rules(["scene/cursor-rules"])`

#### 🔍 代码审查场景
**触发条件：** code review、代码审查、代码检查、审查代码、review代码、检查代码质量、review文件、审查文件、检查文件
**执行动作：**
1. 触发 `code-review-skills` skill
2. 按该 skill 的项目级审查流程执行需求归一化、范围确认、结构化 findings 输出，以及 markdown / HTML 报告生成

#### 📌 需求开发工作流
**触发条件：** 需求开发、功能迭代、需求澄清、方案设计、实施规划，或提供 Flash 需求地址并希望继续推进开发；适用于需要先整理需求、再按既定工作流进入澄清、计划、执行与验证的场景
**不适用情况：** 已命中上述更具体场景的新页面开发、JS转TS、Mars-PC组件库迁移、Forseti接口集成、PRD生成、MDC规则生成、代码审查；以及纯代码调试、纯问题修复、纯代码解释、纯技术问答
**执行动作：**
1. 触发 `requirements-to-superpower` skill
2. 由该 skill 自行完成需求收集、阶段播报、跳转判断，并继续触发后续 superpower skills

### 响应格式【强制使用】

**匹配到特定场景时：**
```
💻 识别到【[场景名称]】场景，正在处理！
🎯 调用规则：[规则名称]
```

**执行保障：**
- 必须先完成场景识别，再选择对应的处理方式
- 场景匹配采用"首次匹配即执行"原则，匹配到一个场景后立即执行，不再继续匹配
- 禁止创建或编造本文档未明确定义的场景

---

## 第二部分：代码生成标准【必须遵循】

### 适用范围
- ✅ 新页面开发：完全按照本标准执行
- ✅ 新功能开发：遵循本标准的技术栈和目录结构
- ✅ 存量代码修改：保持现有风格，同时符合本代码输出标准

### 项目工程标准

#### Vue3项目标准【强制执行】
- **🏗️ 框架基础**：基于MBF框架设计，严格遵循MBF规范
- **🛣️ 路由管理**：建议使用约定式路由，简化配置
- **🔌 接口调用**：必须使用MBF API接口配置及request方法
- **⚡ 技术栈**：Vue3 + TypeScript，禁止使用其他版本
- **🎨 组件库**：必须使用Mars-pc组件库，禁止引入其他UI库
- **📦 状态管理**：使用Pinia

#### Vue2项目标准【灵活适配】
- **🏗️ 框架基础**：基于MBF框架设计，严格遵循MBF规范
- **🛣️ 路由管理**：遵循项目现有路由配置方式
- **🔌 接口调用**：必须使用MBF API接口配置及request方法
- **🎨 组件库**：如果项目已引入shop-mars-pc，则必须使用。否则遵循项目现有UI库
- **📦 状态管理**：使用项目现有的状态管理方案

### 目录结构标准

#### Vue3项目根目录结构【强制执行】
```
src/
├── components/         # 公共组件
├── views/              # 页面视图（或pages命名，对应当前项目结构）
├── apis/               # API接口定义
├── utils/              # 工具函数
├── types/              # TypeScript类型定义
├── store/              # 状态管理（Pinia/Vuex）
├── constants/          # 常量定义
├── assets/             # 静态资源
├── router/             # 路由配置（约定式路由可省略）
├── layout/             # 布局组件
├── composables/        # 组合式函数（Vue3、Vue 2.7+ 中可使用）
└── app.ts              # 应用入口
```

#### Vue2项目目录结构【灵活适配】
- **现有结构优先**：遵循项目当前的目录组织方式
- **渐进式改进**：在不破坏现有结构的前提下，逐步向标准靠拢
- **兼容性保证**：确保新增代码与现有代码风格一致

#### 页面模块目录结构【引用专项规则】
**规则引用：** 页面目录结构和代码组织规范详见 [common-page.mdc](mdc:.cursor/rules/modules/common-page.mdc)
**执行要求：** 所有新页面开发必须严格遵循该专项规则中定义的标准目录结构和代码组织规范

### 代码输出标准【强制遵循】

#### 基础规范【必须严格遵循】
- **🏷️ 命名规范**：[008-code-names.mdc](mdc:.cursor/rules/basic/008-code-names.mdc)
- **📝 注释规范**：[009-comment-rules.mdc](mdc:.cursor/rules/basic/009-comment-rules.mdc)
- **🔒 TypeScript规范**：[006-ts-rules.mdc](mdc:.cursor/rules/basic/006-ts-rules.mdc)
- **💻 通用编码规范**：[003-code-rules.mdc](mdc:.cursor/rules/basic/003-code-rules.mdc)
- **🎨 Vue开发规范**：[005-vue-rules.mdc](mdc:.cursor/rules/basic/005-vue-rules.mdc)
- **🎯 CSS样式规范**：[007-css-rules.mdc](mdc:.cursor/rules/basic/007-css-rules.mdc)

#### 模块规范【必须严格遵循】
- **🌐 API网络请求**：[modules/api-request.mdc](mdc:.cursor/rules/modules/api-request.mdc)
- **🔧 工具函数开发**：[modules/utils.mdc](mdc:.cursor/rules/modules/utils.mdc)
- **🪝 Hooks/Composables**：[modules/hooks.mdc](mdc:.cursor/rules/modules/hooks.mdc)
- **📦 状态管理**：[modules/store.mdc](mdc:.cursor/rules/modules/store.mdc)

---

**本标准是AI代码生成的核心规范，具有场景识别与代码标准双重功能。所有AI生成的代码必须严格遵循本标准，确保代码质量和项目一致性。**
