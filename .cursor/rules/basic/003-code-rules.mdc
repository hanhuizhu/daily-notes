---
description: 通用编码规则
alwaysApply: true
---
# 通用编码规则【强制执行】

## 1. 基本原则【硬性要求】
- **TypeScript优先**：所有新文件必须使用TypeScript，Vue组件script标签必须添加`lang="ts"`
- **ESLint规范**：严格遵循项目ESLint配置

## 2. MBF框架规范【必须遵循】
- **API请求【强制使用】**：必须使用MBF框架的request方法，已在项目中配置完善的拦截器
- **API响应处理【关键规范】**：MBF返回的数据已经是data字段内容，无需再获取response.data，也不用再进行response.code的判断。API响应格式为`{data: {}, message: '', code: 10000}`，但通过mbf调用获取的结果已经是data字段内容，因此直接使用即可。示例：`const data = await queryUserPage(params) as SysUserPageRes; if (data) { tableData.value = data.list || []; }`
- **错误处理【自动化】**：MBF框架已配置全局错误处理，code !== 10000时自动显示错误信息

## 3. 常量管理【强制执行】

### 3.1 定义规范
- 使用对象常量配合`as const`或TypeScript枚举
- 必须包含中文注释说明含义

### 3.2 使用规则
- **字符串常量**：提示文案、状态值、API路径必须定义为常量
- **数字常量**：使用超过2次必须定义为常量
- **枚举定义**：使用TypeScript枚举或常量断言

## 4. 函数编写规范【强制执行】

### 4.1 设计原则
- **公共函数优先**：优先使用项目已有公共函数（日期、字符串、金额处理等）
- **函数原子化**：单一职责，单函数不超过50行
- **纯函数优先**：避免副作用，提高可测试性
- **异步统一**：使用async/await，避免Promise.then链式调用
- **箭头函数优先**：保持代码简洁

### 4.2 核心要求
- 所有函数参数和返回值必须有明确类型
- 优先设计纯函数，确保相同输入产生相同输出
- 异步函数必须使用try/catch处理异常

## 5. 错误处理和边界处理【强制执行】

### 5.1 API请求标准模式
- 使用loading状态控制加载状态
- try/catch捕获异常
- catch中设置空数据默认值
- finally中重置loading状态

### 5.2 数据访问安全
- 使用可选链`?.`访问嵌套属性
- 使用空值合并`||`或`??`设置默认值
- 数组操作前检查是否为空

### 5.3 边界情况
- **空值检查**：所有可能为空的对象和数组必须检查
- **类型验证**：关键数据必须进行类型验证
- **异常捕获**：所有可能异常的代码块使用try/catch
- **默认值设置**：所有可能为空的数据设置合理默认值

## 6. 代码组织和结构【强制执行】

### 6.1 嵌套控制
- 使用早期返回（early return）避免深度嵌套
- if语句嵌套不超过3层
- 所有if语句必须使用花括号，不允许省略

### 6.2 长度限制
- **函数长度**：单函数不超过50行
- **文件长度**：单文件不超过600行，Vue组件不超过300行
- **嵌套层级**：if语句嵌套不超过3层

## 7. 代码复用和模块化【强制执行】

### 7.1 复用规则
- **公共逻辑**：相同逻辑超过2次必须抽取为公共函数
- **组件复用**：相似UI组件必须抽取公共组件
- **常量复用**：重复常量必须抽取到公共常量文件
- **类型复用**：重复类型定义必须抽取到公共类型文件

## 8. 性能优化规范【必须考虑】
- **按需导入**：使用ES6模块按需导入，避免引入整个库
- **懒加载**：大型组件和路由使用懒加载
- **内存管理**：及时清理事件监听器、定时器和订阅
