---
description: TypeScript开发规则
globs:
alwaysApply: true
---
# TypeScript开发规则【必须严格执行】

## 1. 类型声明标准【强制执行】

### 1.1 禁止any类型
- 严禁使用any类型
- 使用unknown代替any作为顶级类型
- 复杂或难以推断的类型使用Record<string, unknown>

### 1.2 类型精确性要求
- 严禁在Vue的template模板中使用TypeScript
- 所有变量必须有精确类型
- 函数参数和返回值必须有显式类型声明
- 严格区分null和undefined
- 使用联合类型(|)表示多种可能的类型
- 遵循项目已有的类型命名和结构规范

### 1.3 复杂类型定义规则
- 优先参考项目现有的类型定义方式和组织结构
- 使用interface定义对象结构
- 重复使用的类型必须创建公共类型定义
- API响应数据必须创建精确的接口定义

### 1.4 API接口定义规范
- 函数返回类型使用`Promise<T>`，不明确则用`Promise<unknown>`
- 请求参数类型必须通过分析调用处获取实际类型结构
- 创建独立的请求和响应类型，如`XxxRequestParams`和`XxxResponse`
- 每个API函数必须有明确的参数类型和返回类型标注
- 根据实际使用情况标注参数的必选/可选性
- 常用API响应结构创建通用类型，如`ApiResponse<T>`
- 避免在接口函数中使用泛型，除非接口需要支持不同数据类型

## 2. 类型文件组织【强制执行】

### 2.1 类型文件抽离规则
- 检查并严格遵循项目统一的类型文件组织方式
- 所有复杂类型必须抽离到独立类型文件（`types.ts`或`*.types.ts`）
- 禁止在vue或js文件内直接定义复杂类型
- 类型文件命名遵循项目规范（如`user.types.ts`、`order.types.ts`）
- 组件相关类型放在组件目录下的types文件夹或遵循项目结构
- 通用类型放在共享目录（如`src/types`）
- 使用命名导出（named export）
- 必须在原文件中正确导入并使用这些类型

### 2.2 类型文件结构规范
- 遵循项目已有的类型文件组织方式
- 每个类型文件职责明确，按功能或业务实体分组
- 单个类型文件不超过200行
- 相关类型放在同一文件，不相关类型禁止混合
- 包含适当注释，解释复杂类型的用途和结构

## 3. Vue项目TypeScript适配【强制使用setup语法】

### 3.1 强制要求
- **严格禁止选项式API**：禁止Vue.extend、class组件、选项式对象等非组合式API
- **严格禁止defineComponent**：Vue2.7和Vue3都不使用defineComponent包装
- **必须使用setup语法**：Vue2.7+和Vue3所有新组件必须使用`<script setup lang="ts">`

### 3.2 setup语法类型安全要求
- Props使用泛型接口严格定义：`defineProps<Props>()`
- Emits明确定义事件类型和参数：`defineEmits<{...}>()`
- ref和reactive的类型必须明确指定或可推导
- 所有组合式函数返回值必须有明确类型声明
- 组件名称通过文件名体现，无需显式name属性

## 4. 高级类型应用【按需使用】
- 泛型`<T>`、交叉类型(&)、条件类型、映射类型
- 优先采用项目已有的类型工具和辅助函数

## 5. Lint错误处理规则【强制执行】

### 5.1 修改限制
- 单文件最高修改5次，严禁循环修改陷入死循环

### 5.2 可忽略的Lint错误
发现lint错误后查看项目类似组件的解决方案，符合以下情况可忽略：
- 与TypeScript类型和功能无关的lint错误
- Vue版本下被弃用但功能正常的API（如.sync修饰符）
- 模块导入或文件导入导致的错误
- 项目配置导致的错误（路径别名、特定规则配置等）
- Vue实例中不存在某属性的错误

### 5.3 特殊处理
- 禁止在不清楚文件后缀时修改导入文件的后缀
- template中slot-scope错误使用#default语法修复
- 类型断言导致lint错误时取消类型断言，移除未使用的类型声明
