---
description: 代码注释规范
alwaysApply: true
---

# 代码注释规范【强制执行】

## 核心原则【必须遵循】
1. **功能导向**：说明代码功能和用途,避免描述显而易见的内容
2. **简洁明确**：内容精炼,避免冗余
3. **中文优先**：使用中文便于团队理解

## 函数注释【强制执行】

### 标准格式
```javascript
/**
 * 函数功能描述
 * @param {类型} 参数名 - 参数描述
 * @returns {类型} 返回值描述
 */
```

### 注释要求
- 复杂函数必须有完整JSDoc注释
- 简单函数可使用单行注释说明用途
- 参数和返回值必须标注类型和含义

## 业务逻辑注释【强制执行】

### 复杂逻辑
- 在关键业务逻辑前添加注释说明意图
- 复杂计算、条件判断必须注释
- 行内注释说明特殊值或规则

### 条件判断
- 非显而易见的条件判断必须注释
- 说明判断的业务含义而非代码本身

## 常量和枚举注释【强制执行】

### 常量注释
- 每个常量必须有行内注释说明用途
- 相关常量可分组并添加组注释

### 枚举注释
- TypeScript枚举必须有文档注释
- 每个枚举值必须有行内注释说明含义

示例:
```typescript
/**
 * 订单状态枚举
 */
enum OrderStatus {
  PENDING = 'pending',     // 待处理
  COMPLETED = 'completed'  // 已完成
}
```

## Vue组件注释【强制执行】

### Props注释
- 每个prop必须有行内注释说明用途
- 格式: `propName: { // 中文说明`

### Data/Ref注释
- 响应式数据必须有行内注释
- 复杂对象可添加多行注释说明结构

### Composition API
```typescript
// Props定义
const props = defineProps<{
  userId: string;  // 用户ID
}>();

// 响应式数据
const loading = ref(false);  // 加载状态
const searchForm = reactive({ // 搜索表单
  keyword: ''                 // 关键词
});
```

## 禁止的注释【严格禁止】
- 无意义的重复代码内容: `// 定义变量`、`// 调用函数`
- 注释掉的废弃代码(应直接删除)
- 过时的注释(修改代码时必须同步更新注释)
