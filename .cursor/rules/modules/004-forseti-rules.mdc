---
description: 使用 forseti 开发规则
alwaysApply: false
---
# 使用 forseti 开发规则【强制执行】

使用获取链接中的 API 文档的内容，需要**严格按照**下面的步骤执行:

## 强制执行顺序【不可跳过】
1. 步骤执行时必须明确告知用户当前正在执行的步骤
2. 每个步骤完成后必须展示执行结果
3. 全部步骤必须按顺序完成
4. 任何步骤失败都必须终止整个流程

## 第一步：熟悉MBF API规则【必须执行】
**输出格式：** `[FORSETI步骤1：MBF文档学习]`

1. 检查是否有 [docs-useage-config-api.md](mdc:.cursor/llms/mbf-docs/docs-useage-config-api.md) 文件
2. 如果有，必须完全读取该文件并熟悉MBF API的配置规则
3. 必须理解文档中的字段命名、结构和配置方式
4. 必须明确展示学习结果，包括：
   - 关键配置字段（swimlane、appId、apiList、appEnv等）
   - 配置格式要求
   - API生成命令及参数

## 第二步：检查框架类型【必须执行】
**输出格式：** `[FORSETI步骤2：框架检查]`

1. 检查 package.json 文件中是否存在 '@tuhu/mbf' 的依赖，这表明项目使用了 mbf 框架
2. 如果存在，必须遵循 MBF 框架的方式添加 API，禁止使用任何其他方式
3. 如果不存在，则检查项目中是否存在 'api'、'apis'、等请求相关的文件夹
4. 必须明确展示检查结果，包括：
   - 是否使用mbf框架
   - 找到的API相关目录结构

## 第三步：检查API生成配置【必须执行】
**输出格式：** `[FORSETI步骤3：API配置检查]`

1. 必须检查项目中是否存在 '.mbfrc.api.ts' 文件
2. 如果存在，必须检查其内容，确认现有的API配置结构
3. 必须严格按照第一步学习的文档规范检查文件格式是否正确
4. 必须明确展示检查结果，包括：
   - 是否找到'.mbfrc.api.ts'文件
   - 如果找到，展示文件的关键结构（namespace、appId、swimlane、appEnv等）
   - 配置是否符合MBF文档规范

## 第四步：根据检查结果处理【必须执行】
**输出格式：** `[FORSETI步骤4：API集成]`

### A. 使用MBF框架的情况：
#### 如果不存在'.mbfrc.api.ts'文件：
1. 明确询问用户提供appid对应的'namespace'的值，必须等待用户回答
2. 根据用户提供的信息和第一步学习的MBF文档规范，创建'.mbfrc.api.ts'文件
3. 配置文件中的字段名称必须与MBF文档中的名称完全一致
4. 必须填充swimlane、appId、apiList、appEnv等相应信息
5. 必须使用'npx mbf g api'命令生成API代码
6. 如果命令执行失败，立即终止整个流程，不进行任何其他尝试
7. 展示生成结果，包括生成的文件路径和内容摘要

#### 如果已存在'.mbfrc.api.ts'文件：
1. 仔细检查API接口文档中的appid是否存在于'.mbfrc.api.ts'文件中
2. 如果存在相同appId：
   - 必须检查swimlane、appEnv和项目内的appId配置是否一致
   - 如不一致，必须仅修改appEnv和swimlane参数，保持一致
   - **严禁删除或修改该appId下已有的任何api url和路径配置**
   - **只允许在原有配置基础上添加新的api url，不得修改或删除已有的api url**
3. 如果不存在相同appId：
   - 必须明确询问用户提供appid对应的'namespace'的值，namespace不要自己编造
   - 必须等待用户明确提供值后再继续
4. 更新'.mbfrc.api.ts'文件，严格按照第一步学习的MBF文档规范
5. 配置名称字段必须与MBF文档中的完全一致
6. 添加新的API配置，必须包含swimlane、appId、apiList、appEnv等相应信息
7. 只添加已在文档中明确定义的API，不添加推测的API
8. 必须使用'npx mbf g api'命令生成API代码
9. 如果命令执行失败，禁止再尝试其他方式创建api请求
10. 命令失败时必须立即终止整个流程，不进行任何其他尝试，告知用户失败原因
11. 执行成功后，必须检查生成的API文件，确认API已正确生成

### B. 不使用MBF框架的情况：
1. 根据Forseti文档创建相应的类型定义和API调用函数
2. 确保类型定义准确反映API的请求和响应结构
3. 如果项目使用TypeScript，必须创建接口类型定义文件和请求方法文件
4. 在现有API调用模式基础上实现新的API

## 第五步：验证集成结果【必须执行】
**输出格式：** `[FORSETI步骤5：验证结果]`

1. 列出所有新创建或修改的文件路径
2. 展示关键API函数和类型定义的实现
3. 确认API集成是否成功完成
4. 总结API的使用方法，方便用户在后续开发中调用

## 关键注意事项【强制遵守】
1. 必须先完全读取MBF API文档，确保理解配置规则
2. 如果是MBF项目，必须且只能使用MBF命令生成API
3. MBF命令失败时，必须立即终止所有流程，不尝试其他方式
4. .mbfrc.api.ts配置的所有字段名称必须与MBF文档中的完全一致
5. 当存在相同appId时，必须确认并保持swimlane和appEnv配置一致
6. **当存在相同appId时，严禁删除或修改已有的api url，只能修改appEnv和swimlane配置**
7. 当需要添加新appId时，必须明确询问用户提供namespace值
8. 一个forseti链接只对应一个接口，在'.mbfrc.api.ts'文件中，只会生成一个接口的代码
9. 不要在'.mbfrc.api.ts'中新增不存在的接口，否则'npx mbf g api'命令会失败
10. 包含必要的错误处理
11. 遵循项目现有的命名和结构习惯
12. 验证结果必须具体明确，包含文件路径和代码示例