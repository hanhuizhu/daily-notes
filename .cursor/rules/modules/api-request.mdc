---
description: API网络请求规范
alwaysApply: false
---
# API网络请求规范【强制执行】

## 接口方法使用优先级【强制】

1. **优先使用** `src/apis/` 目录下已定义的接口方法
2. **参考实现**：若无对应接口，参考项目内其他模块的实现方式
3. **统一规范**：新增接口必须遵循项目现有的定义规范和目录结构

## MBF框架响应处理【关键】
- **重要说明**：MBF返回的数据已经是data字段内容，无需再获取response.data，也不用再进行response.code的判断
- **API响应格式**：`{data: {}, message: '', code: 10000}`
- **实际获取结果**：通过MBF调用获取的结果已经是data字段内容，因此直接使用即可
- **错误处理**：MBF框架已配置全局错误处理，code !== 10000时自动显示错误信息

## 实现规范【必须遵循】

### 查询类接口
- 必须包含 `loading` 状态控制
- 使用 `try/catch/finally` 结构
- catch块处理数据重置等特殊逻辑
- 直接使用返回的data字段：`const data = await queryApi(params)`

### 操作类接口
- 必须包含 `submitting` 状态控制
- 成功后使用 `MrpMessage.success()` 提示
- 通过事件通知父组件刷新：`this.$emit('success')`
- 避免在catch块重复处理已被全局拦截的错误
