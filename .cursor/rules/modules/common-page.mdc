---
description: 通用页面开发规则
alwaysApply: false
---
# 通用页面开发规则【必须严格执行】

## 核心原则【强制遵循】
1. **标准化目录结构**：所有新页面必须遵循统一的目录结构规范
2. **代码分类存放**：不同类型的代码必须放置在指定的目录和文件中
3. **规范严格遵循**：各类型代码必须遵循对应的专项规则
4. **职责清晰分离**：确保各模块职责单一，便于维护和复用

## 页面模块目录结构【新页面开发强制执行】

### 适用场景
**所有新页面开发必须遵守下方标准目录结构，不去参考项目内其他模块的目录结构**

### 标准目录结构
```
views或pages/[模块名]/
├── composables/        # 组合式函数目录(如有)
│   └── usePageLogic.ts
├── components/         # 页面私有组件
│   ├── DialogComponent.vue
│   └── FormComponent.vue
├── types/             # 页面类型定义
│   ├── index.ts
│   └── xxx.types.ts
├── utils/             # 页面工具函数
│   ├── helper.ts
│   └── validator.ts
├── constants.ts       # 页面常量（表格列配置、枚举等）
└── index.vue          # 页面主文件
```

## 代码组织规范【强制执行】

### 1. 页面主文件（index.vue）【必须遵循】
- **文件职责**：页面的主要入口文件，负责整体布局和核心逻辑

### 2. 组合式函数目录（composables/）【条件创建】
- **创建条件**：当页面存在复杂业务逻辑需要抽离或者可以复用时创建
- **职责范围**：
  - 页面状态管理
  - 业务逻辑封装或复用
  - API调用封装
  - 表单验证逻辑
  - 表格数据处理

### 3. 页面私有组件目录（components/）【按需创建】
- **创建条件**：当页面包含可复用或者可拆解的UI组件时创建
- **职责范围**：
  - 弹窗组件（如详情弹窗、编辑弹窗）
  - 表单组件（如搜索表单、编辑表单）
  - 表格组件（如数据表格、操作表格）
  - 其他页面特有的UI组件

### 4. 类型定义目录（types/）【按需创建】
- **创建条件**：所有页面都必须创建此目录
- **文件组织**：
  - `index.ts`：导出所有类型定义
  - `xxx.types.ts`：具体业务类型定义文件
- **职责范围**：
  - 表单数据类型
  - 表格列配置类型
  - 页面状态类型
  - 组件Props类型

### 5. 工具函数目录（utils/）【按需创建】
- **创建条件**：当页面包含可复用的工具函数时创建
- **职责范围**：
  - 数据格式化函数
  - 数据验证函数
  - 业务计算函数
  - 其他页面特有的工具函数

### 6. 常量定义文件（constants.ts）【按需创建】
- **创建条件**：当页面包含表格列配置、枚举、魔法值等情况时创建此文件
- **职责范围**：
  - 表格列配置
  - 状态枚举定义
  - 表单配置项
  - 页面级别的常量
  - 提示文案常量

---

**本规则是新页面开发的核心规范，所有新页面开发必须严格遵循本规则，确保代码结构清晰、职责分离、易于维护。**
