---
description: Vue 数据状态管理规则
alwaysApply: false
---

# Vue 数据状态管理规则【强制执行】

## 触发条件【识别标志】
涉及以下场景时必须触发本规范：
- 状态管理、数据共享、组件通信
- provide/inject、store管理、全局状态
- Vuex、Pinia、跨组件数据、状态同步

## 核心原则【必须遵循】

### 1. 技术栈适配【强制】
- **Vue 2.x项目**：使用Vuex
- **Vue 3.x项目**：优先使用Pinia
- **必须检查项目package.json确定使用的状态管理库**

### 2. 使用场景选择【强制】

#### provide/inject适用场景【轻量级状态】
- 父子组件深层嵌套（避免props逐层传递）
- 组件库内部通信
- 主题配置传递（全局主题、语言等）
- 页面内临时状态共享
- 简单数据传递（不需要复杂操作）

#### Store管理适用场景【复杂状态】
- 全局用户信息（登录状态、权限信息）
- 应用配置信息（系统配置、菜单权限）
- 跨页面状态（购物车、消息通知）
- 复杂业务状态（需要多种操作）
- 需要持久化的状态（localStorage缓存）
- 异步数据管理（API请求的数据缓存）

### 3. 选择决策原则【必须遵循】

| 评估维度 | provide/inject | Store管理 |
|---------|---------------|----------|
| **数据复杂度** | 基础类型、配置对象 | 需要增删改查操作 |
| **作用域** | 页面级别（当前页面及子组件） | 应用级别（跨页面、跨模块） |
| **生命周期** | 临时状态（组件销毁后不保留） | 持久状态（应用生命周期内保持） |
| **操作复杂度** | 只读或简单更新 | 异步处理、数据转换 |

## 目录结构规范【强制】

### Vuex目录结构
```
src/store/
├── index.js              # 导出和初始化
├── modules/              # 按模块组织
│   ├── user.js
│   ├── product.js
│   └── cart.js
├── mutation-types.js     # Mutation常量
└── plugins.js            # Vuex插件
```

### Pinia目录结构
```
src/stores/
├── index.ts              # 导出和初始化
├── modules/              # 按模块组织
│   ├── user.ts
│   ├── product.ts
│   └── cart.ts
└── types/                # 类型定义
    └── index.ts
```

### 模块命名规范【必须遵循】
- **业务模块**：按业务功能命名（`user.js`、`order.js`、`product.js`）
- **功能模块**：按功能类型命名（`auth.js`、`permission.js`、`config.js`）
- **通用模块**：按数据类型命名（`common.js`、`cache.js`、`ui.js`）

### 文件组织原则【强制】
- **单一职责**：每个模块只负责一个业务领域
- **模块化拆分**：单个store文件超过200行需拆分
- **统一导出**：所有模块必须在index文件中统一导出
- **类型集中**：Pinia项目的类型定义统一放在types目录

## 实现规范【强制】

### provide/inject实现要点
```javascript
// 父组件
provide() {
  return {
    config: this.config,              // 静态数据
    theme: () => this.theme,          // 响应式数据（Vue 2.x需用函数）
    updateTheme: this.updateTheme     // 方法传递
  }
}

// 子组件
inject: ['config', 'theme', 'updateTheme']
computed: {
  currentTheme() {
    return typeof this.theme === 'function' ? this.theme() : this.theme
  }
}
```

### Store管理实现要点（Vuex）
```javascript
const userModule = {
  namespaced: true,                    // 必须启用命名空间
  state: { /* 状态定义 */ },
  mutations: { /* 同步修改状态 */ },
  actions: { /* 异步操作 */ },
  getters: { /* 计算属性 */ }
}
```
