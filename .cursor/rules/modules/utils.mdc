---
description: 工具函数Utils开发规范
alwaysApply: false
---
# 工具函数抽取规范【必须严格执行】

## 核心设计原则【强制遵循】

### 1. 分层抽取原则【硬性要求】
- **工程级通用函数【强制抽取】**：与业务无关，可跨项目复用的纯函数
- **业务级函数【选择性抽取】**：与当前项目业务相关，但可在项目内复用的函数
- **页面级函数【不抽取】**：仅在单个页面或组件内使用的函数，保持在原位置

### 2. 复用频次原则【强制执行】
- **≥3次使用【必须抽取】**：相同逻辑在3个及以上位置使用时，必须抽取为公共函数
- **2次使用【建议抽取】**：相同逻辑使用2次时，建议抽取，特别是复杂逻辑
- **1次使用【不抽取】**：仅使用1次的函数不进行抽取，除非预期会复用

## 工程级通用函数【强制抽取到utils目录】

### 存放位置【固定路径】
```
src/utils/
├── dateFormatter.ts      # 日期时间处理
├── stringFormatter.ts    # 字符串处理
├── dataDesensitize.ts    # 数据脱敏
├── countdown.ts          # 倒计时功能
├── moneyFormatter.ts     # 金额处理
├── validator.ts          # 数据验证
├── storage.ts            # 存储操作
├── fileHandler.ts        # 文件处理
└── index.ts              # 统一导出
```

### 工程级函数分类【必须抽取】
- **日期时间处理**：格式化日期、相对时间、日期验证
- **字符串处理**：截断文本、首字母大写、UUID生成
- **金额处理**：金额格式化、金额计算（加减乘除）
- **数据验证**：手机号、邮箱、身份证、空值验证
- **数据脱敏**：手机号、身份证、银行卡脱敏
- **存储操作**：localStorage/sessionStorage封装
- **文件处理**：文件上传、下载、格式转换

## 业务级函数【抽取到pages对应目录】

### 存放位置【按业务模块】
```
src/pages/[模块]/utils/
├── [业务]Formatter.ts     # 业务格式化
├── [业务]Validator.ts     # 业务验证
└── [业务]Calculator.ts    # 业务计算
```

### 业务级函数特征【选择性抽取】
- **业务状态转换**：订单状态、用户状态等业务枚举的转换函数
- **业务规则计算**：运费计算、积分计算等业务逻辑
- **业务数据格式化**：业务对象的展示格式化
- **业务验证规则**：特定业务场景的验证逻辑

## 抽取时机和条件【强制执行】

### 立即抽取【强制执行】
1. **发现重复代码**：相同逻辑出现在不同文件中
2. **函数复杂度高**：单个函数超过30行且逻辑独立
3. **纯函数特征**：无副作用，输入输出明确
4. **通用性强**：可在多个场景使用

### 不抽取【保持原位】
1. **页面特定逻辑**：与特定页面强耦合的函数
2. **一次性使用**：确定只会使用一次的函数
3. **简单操作**：少于10行的简单函数

## 函数设计要求【强制执行】
- **纯函数优先**：无副作用，相同输入产生相同输出
- **单一职责**：每个函数只负责一个功能
- **参数明确**：使用TypeScript明确参数和返回值类型
- **错误处理**：包含完整的错误处理和边界情况
- **文档完整**：提供完整的JSDoc注释

## 导出方式【统一标准】
```typescript
// 具体工具文件 - 命名导出
export const formatDate = () => {}
export const formatDateTime = () => {}

// index.ts - 统一导出
export * from './dateFormatter'
export * from './stringFormatter'

// 使用方式
import { formatDate, maskPhone } from '@/utils'
```
