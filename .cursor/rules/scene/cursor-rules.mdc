---
description: 如何在项目中添加或编辑 Cursor 规则
alwaysApply: false
---

# Cursor 规则管理指南

## 1. 规则文件位置【强制要求】

### 1.1 目录结构
始终将规则文件放在 `PROJECT_ROOT/.cursor/rules/` 目录下，例如：
```
.cursor/rules/
├── scene/                     # 场景化规则目录
│   ├── mars-pc-migration.mdc # Mars-PC迁移规则
│   └── js-to-ts.mdc         # JS转TS场景规则
├── basic/                     # 基础规则目录
└── modules/                   # 模块化规则目录
```

### 1.2 命名规范
- **核心规则**：使用数字前缀 + 短横线命名，如 `001-ai-friendly-standard.mdc`
- **功能规则**：使用功能描述命名，如 `new-page-rule.mdc`
- **场景规则**：放在 `scene/` 目录下，使用 `scene-` 前缀
- **文档规则**：使用 `-docs` 后缀，如 `mars-pc-docs.mdc`
- 文件扩展名必须为 `.mdc`

### 1.3 禁止位置
- 项目根目录
- `.cursor/rules` 以外的目录
- 任何其他位置

## 2. 规则文件结构【标准模板】

```
---
description: 规则用途的简短描述(v版本号)
globs: 可选的路径模式，例如 src/**/*.vue
alwaysApply: true/false
priority: 优先级数字（可选，数字越大优先级越高）
---
# 规则标题【执行级别】

## 触发条件【识别标志】（可选）
当用户出现以下任一表述时，必须触发本规则执行：
- "关键词1"
- "关键词2"
- "相关描述"

## 强制执行原则【必须遵循】
1. **原则1【硬性要求】**：
   - 具体要求描述
   - 执行细节说明

2. **原则2【强制执行】**：
   - 具体要求描述
   - 执行细节说明

## 执行步骤【强制顺序执行】（可选）

### 步骤1: 步骤名称【必须执行】
**输出格式:** `[步骤1: 步骤名称]`

**核心任务:**
- 任务描述1
- 任务描述2

**强制执行:**
- 执行要求1
- 执行要求2

### 步骤2: 步骤名称【必须执行】
**输出格式:** `[步骤2: 步骤名称]`

**核心任务:**
- 任务描述

**强制执行:**
- 执行要求

## 代码示例【必须遵循】

### 正确示例
// 符合规范的代码实现
function correctExample() {
  // 实现细节
}

### 错误示例
// 不符合规范的代码实现
function wrongExample() {
  // 错误的实现方式
}

## 执行验证【必须执行】
每个步骤结束前，必须自我验证是否:
1. 完全遵循当前步骤的所有要求
2. 使用了正确的输出格式
3. 准备好进入下一步骤或完成任务
```

## 3. 规则编写规范【必须遵循】

### 3.1 内容规范
1. **描述清晰**：规则目的和适用场景必须明确
2. **执行级别**：使用【】标注执行级别，如【必须执行】、【强制要求】、【硬性要求】
3. **格式统一**：输出格式使用 `[阶段X: 名称]` 或 `[步骤X: 名称]`
4. **版本管理**：description中包含版本号，如 `(v1.0.0)`

### 3.2 优先级设置
- **9999**：最高优先级（如代码标记规则）
- **1000-9998**：高优先级（核心功能规则）
- **100-999**：中等优先级（场景规则）
- **1-99**：低优先级（文档和辅助规则）
- **不设置**：默认优先级

### 3.3 应用范围设置
- **alwaysApply: true**：始终应用的规则
- **alwaysApply: false**：按需应用的规则
- **globs**：指定文件路径模式，限制规则应用范围
