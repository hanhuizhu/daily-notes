---
description: 存量需求开发规范
alwaysApply: false
---
# 存量需求开发规范【必须严格执行】

## 规则触发条件【识别标志】
对已有业务代码的功能进行迭代，有明确的业务功能点列表（对已有页面新增、修改、删除业务功能点），且功能点列表大于1个。非新页面开发场景，非技术优化、代码优化、代码调试、问题修复、代码解释、问题梳理等场景

## 强制执行标识【核心协议】
- 你**必须**在每个响应开头以方括号声明当前步骤：`[步骤X: 步骤名称]`
- 每个步骤**必须**100%完成后才能进入下一步骤
- 这些规则具有最高优先级，覆盖其他所有指示

## 执行步骤【强制顺序执行】

### 步骤1: 需求分析【必须执行】
**输出格式:** `[步骤1: 需求分析]`

**核心任务:**
- 深入理解用户提出的需求变更内容
- 识别需求涉及的功能模块和业务场景
- 全面分析该需求涉及的代码上下文和依赖文件
- 使用codebase_search和read_file工具深度分析相关代码文件
- 识别所有可能受影响的文件、组件、API接口、配置等

**输出模板:**
```
[需求分析结果]

## 📋 需求理解
**需求描述：** [用户需求的详细描述]
**业务场景：** [涉及的业务场景和用户使用流程]
**变更类型：** [新增功能/修改功能/优化功能/修复问题]
```

**强制执行:**
- 必须使用工具深度分析所有相关代码文件，不得遗漏任何依赖
- 必须完整识别代码上下文和依赖关系
- 完成后自动进入步骤2

### 步骤2: 方案设计【必须执行】
**输出格式:** `[步骤2: 方案设计]`

**核心任务:**
- 根据需求分析结果，制定详细的技术实现方案
- 评估并输出需要新增或者改动的文件计划
- 设计文件修改的优先级和依赖顺序
- **【AI友好设计】** 结合 [001-ai-friendly-standard.mdc](mdc:.cursor/rules/001-ai-friendly-standard.mdc) 规范进行AI友好设计

**AI友好设计要求【必须执行】:**
- **文件长度控制**：Vue组件不超过300行，TS文件不超过600行，函数不超过50行
- **复杂度拆分**：识别并计划拆分复杂函数和大型文件
- **常量提取**：识别重复常量和魔法值，计划提取到constants目录
- **类型文件独立**：复杂类型定义必须抽离到独立的.types.ts文件
- **CSS文件拆分**：大型CSS文件必须按模块拆分
- **原有代码优化**：评估并计划优化原有代码的复杂度和结构

**输出模板:**
```
[技术方案设计]

## 🎯 AI友好设计评估
**现有文件复杂度分析：**
- 文件路径：[路径] - 当前行数：[数量] - 是否需要拆分：[是/否] - 拆分方案：[方案]

**常量和类型提取计划：**
- 魔法值提取：[常量] → 目标文件：[constants/xxx.ts]
- 类型定义提取：[类型] → 目标文件：[types/xxx.types.ts]

## 📁 文件改动计划
**新增文件：**
- 文件路径：[路径] | 文件类型：[类型] | 文件作用：[描述] | 预期行数：[数量]

**修改文件：**
- 文件路径：[路径] | 修改类型：[类型] | 修改内容：[描述] | 修改后行数：[数量]

**删除文件：**
- 文件路径：[路径] | 删除原因：[原因]

**代码优化计划：**
- 函数拆分：[原函数] → [新函数列表]
- 常量提取：[魔法值] → [常量定义]
```

**强制执行:**
- 必须基于步骤1的分析结果制定方案
- **必须进行AI友好设计评估，不得跳过**
- 必须设计合理的执行优先级和依赖关系
- 完成后自动进入步骤3

### 步骤3: TODO计划制定【必须执行】
**输出格式:** `[步骤3: TODO计划制定]`

**核心任务:**
- 根据确认的技术方案，制定详细的TODO执行计划
- 将方案分解为具体的、可执行的任务项
- 为每个TODO项标注优先级、依赖关系和预期结果

**强制执行:**
- 必须使用todo_write工具创建结构化的TODO列表
- 必须将复杂任务分解为可管理的小任务
- 必须为每个任务设置清晰的完成标准
- 完成后自动进入步骤4

### 步骤4: 逐步执行【必须执行】
**输出格式:** `[步骤4: 逐步执行]`

**核心任务:**
- 严格按照TODO计划逐项执行代码修改
- 每完成一个TODO项，立即更新其状态为completed

**强制执行规则:**
- **TODO状态管理【强制执行】**: 每完成一个任务必须立即使用todo_write工具更新状态
- **逐项执行【强制顺序】**: 必须按照TODO的优先级和依赖关系逐项执行，不得跳过或并行执行有依赖关系的任务

**执行流程:**
1. 读取TODO列表，选择下一个待执行的任务
2. 更新TODO状态为completed
3. 重复以上流程直到所有任务完成

### 步骤5: 完成检查【必须执行】
**输出格式:** `[步骤5: 完成检查]`

**核心任务:**
- 检查是否按照TODO列表完成了所有执行项
- 验证所有代码修改是否符合 [001-ai-friendly-standard.mdc](mdc:.cursor/rules/001-ai-friendly-standard.mdc) 的代码生成规则
- 进行功能完整性和正确性验证

**输出模板:**
```
[执行完成检查报告]

## ✅ TODO执行状态检查
**总任务数：** [数量] | **已完成：** [数量] | **未完成：** [数量] | **完成率：** [百分比]

**未完成任务清单：**（如有）
- ❌ 任务X：[任务描述] - [未完成原因]
```

**强制执行:**
- 必须逐项检查所有TODO任务的完成状态
- 必须提供完整的检查报告

## 执行验证【每步骤必检】
每个步骤结束前，必须自我验证是否:
1. 完全遵循当前步骤的所有要求
2. 使用了正确的步骤标识
3. 准备好进入下一步骤或等待用户确认

## 步骤转换记录【强制执行】
在每次从一个步骤转入另一个步骤时，必须在步骤标记下方添加：
```
✅ 前一步骤已完成
⏩ 正在进入新步骤
```

## 特殊情况处理【强制执行】

### 1. 需求不明确
如果用户需求描述不够清晰，必须在步骤1中明确指出并要求用户补充详细信息

### 2. 技术方案复杂
如果涉及的修改过于复杂，必须在步骤2中将方案进一步细分，确保每个子任务都是可管理的

### 3. 执行失败
如果某个TODO任务执行失败，必须在步骤4中暂停，分析失败原因并调整方案

## 违规后果【强制执行】
如违反任何强制执行规则，必须立即停止并重新执行当前步骤，不得继续。特别强调：
- 不按步骤执行
- 不进行充分的需求分析
- 不制定详细TODO计划
- 不进行完成检查
这些都是绝对禁止的行为。

## 成功标准【最终验证】
只有当以下所有条件都满足时，才能认为存量需求开发完成：
1. ✅ 所有5个步骤都已完整执行
2. ✅ 所有TODO任务都已完成
3. ✅ 需求功能已正确实现
4. ✅ 现有功能未受影响
5. ✅ 代码质量符合项目标准

---
