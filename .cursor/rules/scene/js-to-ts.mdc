---
description: JS转TS规则
alwaysApply: false
---
# JavaScript到TypeScript转换规则【必须严格执行】

## 触发条件【识别标志】
当用户出现以下任一表述时，必须触发本规则执行：
- 将JavaScript代码转换为TypeScript
- 添加类型声明/类型定义
- JS转TS/TS重构
- 代码类型安全/类型系统优化
- 明确提到TypeScript相关需求

## 执行前准备【强制步骤】
1. **文件分析【必须执行】**：
   - 识别需要转换的文件类型（.js/.jsx/.vue）
   - 分析文件的导入导出结构
   - 识别组件、函数、变量的使用模式

## 转换规则【强制执行】

### 1. 代码结构完整性【核心原则】
- 保持原有代码结构、逻辑和功能完全不变
- 仅添加类型声明，不改变执行逻辑
- 保留所有原始注释
- **严禁修改既定的函数名称【硬性要求】**：
  - 保持所有函数名、方法名、变量名完全不变
  - 不得以"规范化"为理由修改现有命名
  - 即使命名不符合最佳实践，也必须保持原样
  - 只能添加类型注解，不能重命名任何标识符

### 2. TypeScript规则
必须遵守 [006-ts-rules.mdc](mdc:.cursor/rules/basic/006-ts-rules.mdc) 中的全部规则，该文件包含以下内容：
- 类型声明标准
- 类型文件组织
- Vue项目TypeScript适配
- 高级类型应用
- 对lint错误的处理规则

在进行JS到TS转换的过程中，必须严格遵循006-ts-rules.mdc中定义的所有TypeScript规则，优先按照006-ts-rules.mdc的标准执行，无需考虑与现有项目代码风格的一致性。

## 执行后验证【必须执行】
1. 检查是否有遗漏的类型声明
2. 验证转换后代码不包含any类型
3. 确认所有接口和类型定义有明确的命名
4. 确保转换代码完全符合006-ts-rules.mdc规范，无需考虑现有项目代码风格
5. **验证复杂类型是否已适当抽离到独立类型文件中**
6. **检查类型文件的组织是否符合项目结构和命名规范**
7. **验证命名完整性【强制检查】**：
   - 确认所有原始函数名、方法名、变量名保持不变
   - 检查是否存在任何不必要的重命名
   - 验证只添加了类型注解，未修改任何标识符

## 输出格式【必须执行】
转换完成后，必须提供以下信息：
- 具体执行的转换类型
- 添加的关键类型定义
- 对复杂类型的解释
- 需要用户注意的地方
- **创建的所有类型文件列表及其用途**