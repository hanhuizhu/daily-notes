---
description: 新页面或新功能开发规则
alwaysApply: false
---
# 新页面或新功能开发规则【必须严格执行】

## 强制执行标识【核心协议】
- 你**必须**在每个响应开头以方括号声明当前阶段：`[阶段X: 阶段名称]`
- 每个阶段**必须**100%完成后才能进入下一阶段
- **必须**要先执行前置流程完成后，才可进入开发流程

## 触发条件
当用户提及：新建页面、新功能开发、实现页面、创建功能时触发

## 前置流程【当用户输入包含forseti.tuhu.work链接，强制执行】
**输出格式：** `[前置流程: Forseti API检查]`

1. **接口检测**：检查是否包含forseti链接，如包含则告知用户执行前置流程
2. **流程执行**：按 [004-forseti-rules.mdc](mdc:.cursor/rules/modules/004-forseti-rules.mdc) 执行API请求和TS类型生成，每步完成后才执行下一步。无接口地址则标记"无Forseti前置需求"并进入信息收集
3. **流程验证**：5个步骤全部完成后才能进入开发流程，失败则中止并告知用户

## 开发流程【顺序执行】

### 阶段1: 信息收集
**输出格式:** `[阶段1: 信息收集]`

**任务:**
- 确认前置流程已完成（如适用）
- 分析用户提供的所有信息(含图片)
- 识别关键文件、组件和数据流
- 评估功能影响范围

**约束:** 禁止提出解决方案、规划或实现。收集完成后自动进入阶段2

### 阶段2: 需求确认
**输出格式:** `[阶段2: 需求确认]`

**任务:**
- 详细阐述需求理解
- 如有UI原型图，精确描述元素布局、位置、颜色、大小、宽高、交互和样式
- UI严格按照图片识别的元素，不扩展或删减
- 询问用户需求理解是否准确

**约束:** 禁止提供实现细节或代码。等待用户确认后进入阶段3

### 阶段3: 技术设计
**输出格式:** `[阶段3: 技术设计]`

**代码标准:** 严格遵循 [001-ai-friendly-standard.mdc](mdc:.cursor/rules/001-ai-friendly-standard.mdc)（项目工程、目录结构、基础规范、模块规范）

**任务:**
- 制定详细技术方案：路由设计、api请求、组件库、组件使用、常量抽取、配置化、组件拆分、函数复用，含精确文件路径和函数名，输出目录结构
- 遵循 [common-page.mdc](mdc:.cursor/rules/modules/common-page.mdc) 目录结构规范
- 调用listMrpComponents和listMrbComponents获取组件，优先Mrb物料组件，输出：
```
[使用到的组件]
- 组件名：[shop-mars-pc组件名称]
- 理由：[组件作用在哪里]
```
- 考虑可维护性，单文件≤300行，超出需抽离
- 可参考项目代码结构，但需按实际需求设计
- 提供详细更改计划：
```
[更改计划]
- 文件：[路径]
- 类型：[新建/修改/删除]
- 内容：[具体更改内容]
- 理由：[技术原因]
```
- 询问用户是否同意
- 用户提修改建议时，带方案进行修改，考虑修改的影响，保证方案的完整性和一致性
- 等待用户确认后存储方案

**方案存储:**
- 确认后立即写入`.cursor/plan/技术方案-[功能名称]-[YYYY-MM-DD-HH-mm-ss].md`
- 包含：需求确认阶段的全部内容、接口映射、组件清单、路由设计、api设计、更改计划、文件拆分、路径函数名、实现细节、注意事项
- 告知用户文件名后进入阶段4

**约束:** 禁止编写代码或简化规划。方案变更需重新批准。等待用户确认后立即存储

### 阶段4: 代码实现
**输出格式:** `[阶段4: 代码实现]`

**方案读取:**
- 从`.cursor/plan`读取技术方案文件
- 展示方案内容，确保理解所有细节
- 告知用户："已读取技术方案，开始实现"

**核心任务:**
- **必须先执行的步骤【强制执行】**: 根据阶段3技术设计中确定需要使用的组件，调用mcp工具getMrpComponentsDetail及getMrbComponentsDetail工具（如果包含Mrb业务组件），example为false，获取每个组件的详细用法，确保完全理解每个组件的属性、方法和使用限制
- 明确所有当前阶段的强制执行规则，然后告诉我："我已经明确规则，知道怎么做了，开始编码"，再严格按照批准的计划实现每个文件，开始编码
- 确保每个文件实现完整功能，包括错误处理

**强制执行规则:**
- **代码生成标准遵循【最高优先级】**: 必须严格遵循 [001-ai-friendly-standard.mdc](mdc:.cursor/rules/001-ai-friendly-standard.mdc) 中定义的所有代码生成标准，包括但不限于项目工程标准、目录结构标准、基础规范和模块规范
- **方案严格执行【最高优先级】**: 必须完全按照.cursor/plan目录中存储的技术方案进行实现，不得有任何偏差或自行调整
- **组件使用前置准备【最高优先级】**: 必须先完成getMrpComponentsDetail及getMrbComponentsDetail工具（如果包含Mrb业务组件）的调用，example为false，获取并理解每个需要使用的组件详细用法后，才能开始实际代码编写，禁止在未获取组件详细用法的情况下编写任何组件相关代码
- **严格实现需求【硬性要求】**: 必须完全、精确地按照已确认的需求确认阶段和技术设计阶段的产物实现，不得有任何偏差或自行调整，这是强制执行的硬性规则
- **组件使用规范【硬性要求】**: 使用@tuhu/shop-mars-pc组件库时，必须严格按照getMrpComponentsDetail 以及 getMrbComponentsDetail获取的组件用法实现，一定不要使用组件用法中不存在的属性或者方法，组件名称也要按照组件用法中提供的方式一致命名
- **数据处理规则【强制遵守】**: 当使用mbf生成的api请求进行调用时，response结果直接就是data字段的数据，无需再获取response.data，也不用再进行response.code的判断。API响应格式为`{data: {}, message: '', code: 10000}`，但通过mbf调用获取的结果已经是data字段内容，因此直接使用即可。示例：`const data = await queryUserPage(params) as SysUserPageRes; if (data) { tableData.value = data.list || []; }`，这条规则必须严格遵守，无任何例外
- **UI实现【严格遵循】**: 必须严格根据需求确认阶段中确定的UI布局和元素来实现，不得因接口字段内容而自行调整UI设计
- UI一定要按照识别的元素来进行编写，不要扩展和以自己的想法新增或者减少元素
- 完成实现后自动进入阶段5

**违规处理:** 违反规则立即停止重做。不获取组件用法、不遵循方案、不添加标记、不按mbf规范处理API均为禁止行为

### 阶段5: 代码审查
**输出格式:** `[阶段5: 代码审查]`

**任务:**
- 验证实现是否符合.cursor/plan技术方案
- 验证代码是否遵循 [001-ai-friendly-standard.mdc](mdc:.cursor/rules/001-ai-friendly-standard.mdc) 所有标准
- 验证功能完整性和技术正确性
- 确认UI实现遵循需求确认阶段设计，未根据接口字段调整
- 验证API响应处理：直接使用mbf返回的data，无需response.data获取或response.code判断
- 告知用户："已完成技术方案验证，所有项目均按计划实现"或"发现偏差：[内容]"

**检查项:**
- 先验证技术方案，再审查其他
- 标记与计划的偏差
- 验证代码标记应用于每个文件
- 检查UI与需求确认阶段一致
- 确认API响应处理符合规范
- 确认代码遵循001-ai-friendly-standard.mdc

## 执行验证【每阶段必检】
阶段结束前验证：
1. 遵循当前阶段所有要求
2. 使用正确阶段标识
3. 准备进入下阶段或等待确认

## 阶段转换记录
阶段切换时添加：
```
✅ 前一阶段已完成
⏩ 正在进入新阶段
```
